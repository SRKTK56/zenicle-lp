# app.zenicle.jp を zenicle.jp のトンマナに揃える — 仕様書

対象: tmkn-jp/zenicle `apps/web`（公開ページ・チームコンソール）
基準: https://zenicle.jp （実装 `index.html`）と本ディレクトリのモック
作成: 2026-09-14 ／ 更新: 2026-09-15（#47〜#52 反映済みの実物を確認し、§3.5〜3.7 を追加。**編集画面＝チームページ**と**運営承認フロー**）

---

## 0. 現状診断（コードを読んだ結果）

| 領域 | 現状 | 判定 |
|---|---|---|
| カラートークン（公開ページ） | `globals.css` の `--c-*` / `--zen-*-rgb` はすでに LP と同じ「確定パレット 2026-07-22」（`#F7F9FC` / `#14213D` / `255 138 0`） | **ほぼ揃っている** |
| フォント | 両方 Noto Sans JP | 揃っている |
| **チームコンソール** `(team)/team/console/*` | `bg-zen-void` `text-white` `text-zinc-400` `bg-zen-bgCard` など**旧ダークテーマがハードコード**。トークンを通っていない | **最大のギャップ** |
| `ui/Button` | `primary` が `bg-blue-600`、`zen` variant はダーク前提（`text-[#0a0812]`、`ring-offset-[#0a0812]`） | 要修正 |
| `ui/Card` ほか共通部品 | **訂正（2026-09-15 田村さん実測）**: `ui/Card` はライト固定（`border-zinc-200 bg-white`）。ダーク版は別部品 `ZenCard`。`bg-zen-bgCard` を直接使う `ui/` 部品は無い | 角丸・影・罫線の値合わせのみ |
| 角丸・影 | LP: カード `1.25rem`、ボタン `999px`、影 `0 10px 30px rgba(20,33,61,.08)`。アプリ: `rounded-[10px]`、`shadow-zen-glow-*`（発光系） | 統一する |
| チームページ `/t/[id]` | **訂正**: 既にトークン化済み（778行）。色ではなく**構造の再設計**の仕事 | 再設計（モックあり） |
| ダーク固定が残る範囲 | **訂正**: `team/console` の 8 ファイル 2002 行（約 80 箇所）と `ui/Button` | ここが PR-2 の対象 |

結論: **「色を変える」仕事ではなく、「コンソールをライトテーマのトークンに乗せ、共通部品の見た目を LP に合わせる」仕事**。

---

## 1. トークン対応表

LP の `:root`（`index.html`）と、アプリ `globals.css` の用途別トークンの対応。
値が違う行だけアプリ側を LP に合わせる（★印）。

| 役割 | LP（正） | アプリ現行 | 対応 |
|---|---|---|---|
| ページ背景 | `--bg: #F7F9FC` | `--c-bg-base: #f7f9fc` | 同じ |
| 帯・カード背景 | `--bg-band: #FFFFFF` | `--c-bg-alt` / `--c-bg-card: #ffffff` | 同じ |
| 薄い面 | `--surface-soft: #F4F7FC` | `--c-bg-surface: rgba(20,33,61,.04)` | ほぼ同じ（どちらでも可） |
| 本文 | `--text: #14213D` | `--c-text-body: #14213d` | 同じ |
| 補足文 | `--text-muted: #5B6473` | `--c-text-muted: #374151` | ★ `#5B6473` に |
| 注釈 | `--text-subtle: #8A93A3` | `--c-text-subtle: #6b7280` | ★ `#8A93A3` に |
| 罫線 | `--border: rgba(20,33,61,.10)` | `--c-border-line: rgba(20,33,61,.12)` | ★ `.10` に |
| 強い罫線 | `--border-strong: rgba(20,33,61,.14)` | `--c-border-strong: rgba(20,33,61,.22)` | ★ `.14` に |
| 主役色 | `--brand: #FF8A00` | `--zen-orange-rgb: 255 138 0` | 同じ |
| アクセント | `--pink: #FF4D8D` | `--zen-pink-rgb: 255 77 141` | 同じ（達成率・CUPにだけ使う） |
| 主役グラデ | `linear-gradient(135deg,#FFA726,#FF8A00)` | `--c-grad-orange-from/to` | 同じ |
| 文字グラデ | `linear-gradient(135deg,#FF8A00,#FF4D8D)` | なし | ★ 追加（`--c-grad-text`）。数値・強調語にだけ使う |
| カード影 | `0 10px 30px rgba(20,33,61,.08)` | `shadow-zen-glow-*`（発光） | ★ 追加 `--c-shadow-card`。発光系は使わない |
| ホバー影 | `0 18px 44px rgba(20,33,61,.14)` | なし | ★ 追加 `--c-shadow-card-hover` |
| ボタン影 | `0 10px 30px rgba(255,138,0,.28)` | `0 12px 50px rgba(245,158,11,.45)` | ★ 値を差し替え |
| フッター | `--footer-bg: #14213D` | — | ネイビー地・白文字（LP と同じ） |

**ダークテーマ（`data-theme='dark'`）は当面「維持だけ」**。新規の見た目調整はライトだけで行い、ダークは壊れていなければ触らない。

---

## 2. 共通コンポーネント指針（`src/components/ui`）

| 部品 | LP の見た目（正） | 変更点 |
|---|---|---|
| **Button primary** | オレンジグラデ `#FFA726→#FF8A00`、白文字 `font-black`、**pill（999px）**、影 `0 10px 30px rgba(255,138,0,.28)`、hover `scale(1.05)` | `primary` を `zen` 相当に置き換え、`bg-blue-600` を廃止。`text-[#0a0812]`→白、`ring-offset-[#0a0812]`→`ring-offset-white` |
| **Button ghost/outline** | 白地・`border: 1px solid rgba(20,33,61,.14)`・ネイビー文字・pill | `zinc-*` を `--c-*` に |
| **Card** | 白・`1px solid var(--c-border-line)`・**角丸 1.25rem**・`--c-shadow-card`。hover で `translateY(-4px)`＋ボーダーがオレンジ | `bg-zen-bgCard` / `border-zen-line` を廃止 |
| **見出し** | セクション見出しは `font-black`（900）。ラベルは `.68rem` `letter-spacing:.2em` `uppercase` オレンジ | `font-semibold`（600）中心 → 見出しは 900、本文は 400/500 |
| **Badge** | pill、`.72rem` `font-black`。塗りはオレンジ 12% 地＋オレンジ文字（状態バッジ） | 色の意味を絞る：受付中＝オレンジ、終了＝グレー、注意＝ピンク |
| **Input / Textarea** | 白地・`border-line`・角丸 `.75rem`・focus でオレンジ枠 | ダーク前提の `bg-zen-bgCard text-zinc-200` を廃止 |
| **Table** | 罫線 `border-line` のみ。ゼブラなし。数値は `tabular-nums` 右寄せ | — |
| **Modal** | 白・角丸 `1.25rem`・背景 `rgba(10,8,18,.82)`＋blur | — |
| **Skeleton** | `linear-gradient(90deg, surface-soft, #fff, surface-soft)` の流れるアニメ | — |
| **数値の強調** | `gold-text`（文字グラデ）。累計額・達成率・実績数値にだけ | 乱用しない |

---

## 3. チームコンソール（`(team)/team/console`）

### 3.1 テーマ
- `bg-zen-void` / `bg-zen-bgCard` / `bg-zen-bgOverlayDark` / `text-white` / `text-zinc-*` / `hover:bg-white/5` を**すべて `--c-*` トークンに置換**し、ライトテーマにする（公開ページと同じ地）
- サイドバー: 白地、選択中は `bg-orange-500/10 text-orange-600 font-bold`、それ以外は `--c-text-muted`
- ヘッダー: 白地・下罫線 `border-line`。「チームコンソール」の h1 はネイビー

### 3.2 情報設計 — ページの構造 = 編集の構造
困りごと: **「どこを編集すれば公開ページのどこに出るか」が分からない。**
対策は3つ。順に効く。

1. **サイドバーを公開ページのセクション名・順番と完全に一致させる**（下表）
2. **各編集画面の冒頭に「チームページの『○○』に表示されます」＋そのセクションのプレビュー画像（縮小スクショで可）**を置く
3. **公開ページにメンバー限定の「✎ 編集」導線**を出す（モック `team.html?edit=1` の青いチップ）。クリックで該当コンソール画面へ

### 3.3 ページ ↔ 編集画面 対応表（サイドバーの順番そのもの）

| # | 公開チームページのセクション | コンソールのメニュー名 | 編集項目 | 現在の置き場 |
|---|---|---|---|---|
| — | カバー画像・チーム名 | **ヘッダー** | iconUrl（21:9）、name | 設定「表示・公開」 |
| 01 | プロジェクト説明動画 | **プロジェクト** | pitchVideoUrl、goalAmount | 設定「プロジェクト」（新設済） |
| 応援 | 累計・目標・達成率・応援ボタン・シェア | （自動） | 表示のみ。目標額は「プロジェクト」で | — |
| 02 | 活動紹介・メッセージ | **活動紹介** | description、thanksMessage（#47 で teams/:id に追加） | 設定「表示・公開」／シーズン設定 |
| 03 | メンバー紹介 | **メンバー** | メンバー、役割、プロフィール（avatar/bio） | メンバー |
| 04 | エールの使い道 | **使い道（支出）** | fundUsage | 支出 |
| 05 | 応援してくれている人 | （自動） | 表示のみ | — |
| 06 | 活動レポート | **活動レポート（投稿）** | progressPosts、支援者限定 reports | 投稿／レポート |
| 末尾 | SNS リンク | **SNS** | webUrl / xUrl / instagramUrl / youtubeUrl / otherUrl | 設定「SNS」 |
| — | 公開設定・誓約 | **公開設定** | status、コンプライアンス | 設定「表示・公開」「コンプライアンス」 |

「（自動）」の項目もサイドバーに**読み取り専用で並べる**（「自動で表示されます」と書く）。編集できないことを隠すと、探し続けてしまう。

### 3.4 コンソールトップ（`/team/console`）
「やること」を上に、「数字」を下に。
- 上段: 未完了の設定をチェックリストで（動画URL未登録／目標額未設定／使い道の登録が0件／今週の投稿なし）。各行から該当画面へ
- 中段: 公開ページの縮小プレビュー（iframe で `/t/<id>` を表示。編集後すぐ確認できる）
- 下段: 累計・応援者数・シーズン残日数

#### 2026-09-15 実物（#51 反映後）へのフィードバック
- ✅ ライト化・サイドバーの一致・「自動」ラベル・「やること」チェックリスト・「公開ページを見る」は仕様どおり
- ❌ 下段の5枚のカード（メンバー一覧／メンバー表示／進捗投稿／レポート／設定）は**旧名称のままの二重導線**。削除（残すならサイドバーと完全一致の名称に）
- ❌ 「所属チーム」ブロックの**「参加先プログラムID」生入力欄**はトップに置かない。チーム切替はヘッダーのプルダウンで足りる。プログラム参加は「受付中プログラムから選ぶ」画面へ分離
- ❌ 数字（累計・応援者数・残日数）が無い。「やること」の直下に置く
- △ サイドバー末尾の「メンバー表示」「リワード」は区切り線＋「その他」で分ける
- △ 「やること」は動画・目標額の2件のみ。使い道0件／投稿なし も対象に

### 3.5 編集画面はチームページそのもの（方針転換・2026-09-15）
コンソールに「セクション別の編集画面」を持つ設計（§3.2〜3.3）は**やめる**。理由:
**応援者と同じ画面を見ながら編集するほうが、何が足りないか・どうすれば魅力的になるかにチーム自身が気づける**から。

- チーム管理者・メンバーでログインすると、`/t/[id]` に「編集モード」の切り替えが出る（モック `team.html?edit=1`）
- 編集モードでは各セクションの右上に「✎ 編集」。押すと**右ドロワー**でそのセクションの項目だけを編集（ページを離れない）。ドロワー冒頭に必ず「チームページの『○○』に表示されます」
- **空欄のセクションは、応援者に見える位置に「＋ 追加」の枠**で出す（「＋ 説明動画を登録する」「＋ 使い道を登録する」…）。空欄を隠さないことが気づきの仕掛け
- 目標額は応援レール内に「＋ 目標額を設定する」
- コンソールに残すのは: トップ（やること・数字・承認状況）／活動レポートと使い道の**一覧管理**（並べ替え・削除）／メンバー招待／公開設定・誓約／リワード。**文言・画像・リンクの編集はすべてページ側**
- スマホでも同じ（ドロワーは全幅のボトムシート）

### 3.6 メンバー情報を管理者が編集できる
- 現状: メンバー本人だけが自分のプロフィール（表示名・アイコン・自己紹介・SNS）を編集できる
- 変更: **チームの管理者（`TeamMember.role = admin`）は全メンバーの表示名・アイコン・自己紹介・SNS リンクを編集できる**。編集モードの各メンバーカード右上の「✎」から（モック参照）
- API: `PATCH /teams/:teamId/members/:memberId/profile` を admin にも許可（本人 or admin）。監査用に `updatedByUserId` を残す

### 3.7 公開には必ず運営の承認が要る（承認フロー）
チームが編集した内容は**保存しても公開されない**。ゼニくる運営の承認後に公開する。

| 状態 | 誰が | 何が起きる |
|---|---|---|
| 下書き | チーム | 「✎ 編集」→保存。公開ページは**前の内容のまま**。編集モードでは該当セクションに「未公開の変更」バッジ、上部バーに件数 |
| 承認待ち | チーム→運営 | 「公開を申請する」でまとめて申請。バッジが「承認待ち」に。申請中は追加の編集も下書きに積める |
| 承認／差し戻し | 運営 | 運営コンソールの審査キューで**変更前後の差分**を見て承認 or 差し戻し（理由つき）。承認で公開ページが更新される |

- 承認の単位は「申請1回＝変更セクションの束」。セクション単位で部分承認できると運営が楽（任意）
- **対象は、チームページに表示される変更すべて**（文言・画像・動画URL・目標額・メンバー情報・SNS・使い道・活動レポート）
- 現状の `Team.status`（DRAFT / PENDING_REVIEW / PUBLISHED）は「ページの存在」の審査。これとは別に**「内容の変更」の審査**を持つ（`TeamPageRevision` のような下書き＋審査履歴）
- 運営側: 審査キュー（未処理件数・チーム名・申請日時・変更セクション）、差分表示、承認/差し戻し、通知（Discord or メール）
- チーム側: 状態が常に見える（未公開の変更 n 件／承認待ち／差し戻し理由）
- 注意: 活動レポートまで承認制にすると更新の鮮度が落ちる。運営の負荷を見て「投稿は事後確認」に緩める余地を残す（設定で切り替え可能に）

---

## 4. チームページ `/t/[teamId]` — モック `team.html`

- 実装の正: `design/team.html`（実 API で描画。`?team=<id>` で任意チーム、`&edit=1` で編集モード＝ドロワー編集・空欄枠・承認フローの状態表示）
- セクション順（確定）: **カバー → 01 動画 → 応援（目標・累計・達成率・応援ボタン・X/Threads/LINE/URLコピー） → 02 活動紹介・メッセージ → 03 メンバー → 04 使い道 → 05 応援者 → 06 活動レポート**
- PC: 本文 8 カラム＋右 4 カラムの応援レール（sticky）。スマホ: 動画の直後に応援ブロック、画面下に固定 CTA バー
- 空状態は「まだ登録されていません。登録すると○○が表示されます」と、次の行動が分かる文にする
- 数値の色: 累計額だけ `gold-text`、達成率だけピンク。それ以外はネイビー
- 解決済み（#47）: thanksMessage は teams/:id に解決済みの値を追加、支援者限定レポートは件数のみ追加（タイトル・日付は出さない）

---

## 4.1 数値表示の規則（2026-09-15 追加・田村さん提案）

- `totalAmount` は「順位表を公開しているプログラム」のみの合算。順位表を隠しているプログラムにしか参加していないチームは `totalAmount = 0` のまま
- したがって **`totalAmount = 0` のときは達成率ブロック（目標額・％・進捗バー）ごと非表示**にする。0% と出すと誤解を生む
- LP（`index.html` のチームカード）とモック（`team.html`）も同じ規則に揃えた

## 4.2 API 側の前提（Issue #47）

- `thanksMessage` の実体は `SeasonTeam.rewardOverrides` の1キー（シーズン×チーム単位）。未認証で取得でき、現行 `/t/[id]` は `GET /seasons/:id/reward-plans?seasonTeamId=` で表示している。往復を減らすため `GET /teams/:teamId` に解決済みの値を追加する（どのシーズン分を出すかの規則は API 側で決める）
- 支援者限定レポートは件数だけ `GET /teams/:teamId` に追加（タイトル・日付は出さない）
- `pitchVideoUrl` / `goalAmount` / `totalAmount` / `supporterCount` も `GET /teams/:teamId` に追加（現状は team-explore にしか無い）

## 5. 進め方（Issue と順番。2026-09-15 確定）

| 順 | Issue | 内容 | 備考 |
|---|---|---|---|
| 0 | **#47** | API 追加（§4.2） | チームページ再設計の前提 |
| 1 | **#48** | トークン＆共通部品（§1・§2） | `ui/Button` が主対象 |
| 2 | **#49** | コンソールのライト化（§3.1） | 8 ファイル・約 80 箇所 |
| 3 | **#50** | チームページ再設計（§4） | モック `team.html` が正 |
| 4 | **#51** | コンソールの情報設計（§3.2〜3.4） | 反映済み。§3.4 のフィードバック対応が残り |
| 5 | 新規 | **チームページ上でのその場編集**（§3.5） | 編集モード・ドロワー・空欄枠。コンソールの編集画面は一覧管理に縮小 |
| 6 | 新規 | **管理者によるメンバー情報編集**（§3.6） | API の権限追加＋メンバーカードの「✎」 |
| 7 | 新規 | **運営承認フロー**（§3.7） | 下書き→申請→運営審査→公開。運営コンソールに審査キュー |

各 PR の受け入れ基準は「zenicle.jp と並べて違和感がないこと」＋「対応表どおりに編集先へ辿れること」。
